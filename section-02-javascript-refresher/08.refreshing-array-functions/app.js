const numbers = [1, 2, 3, 4, 5];

// map executes a function on every element on an array
const doubleArray = numbers.map((num) => {
  return num * 2;
});

console.log(numbers);
console.log(doubleArray);

/**
 * Destructuring extracts all elements and all properties and distributes them in a new
 * object or array. These can be stored in variables.
 */

// array destructuring
const [a, b] = ["Max", "Andy"];
console.log(a);
console.log(b);

//object destructuring
let { name } = { name: "Andy", age: 28 };

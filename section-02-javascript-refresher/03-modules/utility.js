export const clean = () => {
  console.log(
    "This is a function that says clean. It doesn't do anymore that that Just cleans lol. I want this to be a long string to test VSCode wrap."
  );
};

export const baseData = 10 / 22 / 2022;
